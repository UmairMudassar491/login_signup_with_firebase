import 'package:flutter/material.dart';

class QariPage extends StatelessWidget {
  const QariPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Qari Page", textAlign: TextAlign.center);
  }
}
