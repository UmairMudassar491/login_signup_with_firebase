import 'package:flutter/material.dart';

class StudentLessonsTab extends StatelessWidget {
  const StudentLessonsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Student Lessons Tab'),
    );
  }
}
