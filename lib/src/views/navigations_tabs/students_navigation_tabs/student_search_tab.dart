import 'package:flutter/material.dart';

class StudentSearchTab extends StatelessWidget {
  const StudentSearchTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Student Search Tab'),
    );
  }
}
