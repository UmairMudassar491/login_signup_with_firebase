import 'package:flutter/material.dart';

class StudentMessagesTab extends StatelessWidget {
  const StudentMessagesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Student Messages Tab'),
    );
  }
}
