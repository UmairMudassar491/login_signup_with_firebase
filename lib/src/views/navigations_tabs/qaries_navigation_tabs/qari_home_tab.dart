import 'package:flutter/material.dart';

class QariHomeTab extends StatelessWidget {
  const QariHomeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Qari Home Tab'),
    );
  }
}
