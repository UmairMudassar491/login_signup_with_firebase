import 'package:flutter/material.dart';

class QariSearchTab extends StatelessWidget {
  const QariSearchTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Qari Search Tab'),
    );
  }
}
