import 'package:flutter/material.dart';

class QariMessagesTab extends StatelessWidget {
  const QariMessagesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Qari Messages Tab'),
    );
  }
}
